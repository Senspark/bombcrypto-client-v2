namespace App {
    public enum HeroStage {
        Working, Sleep, Home
    }

    public enum HeroRarity {
        Common, Rare, SuperRare, Epic, Legend, SuperLegend, Mega, SuperMega, Mystic, SuperMystic
    }
    
    public enum HeroSAbilityType {
        AvoidThunder,
    }
}