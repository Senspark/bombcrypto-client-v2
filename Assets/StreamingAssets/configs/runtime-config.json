{
  // Config sửa được SAU build (StreamingAssets, không bị nén vào .data).
  // React ghi đè file này ở build time; trong Editor sửa rồi Play, không cần rebuild.
  // landing: "treasure" => FarmingScene | "adventure" => MainMenuScene
  "landing": "treasure"
}
